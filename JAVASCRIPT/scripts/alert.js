#!/usr/bin/node

let icecream = "strawberry";

/*
 * use the strict equality operator to compare the var
 * icecream to the string strawberry.
 * Checks whether they are equal.
 */

if (icecream === "strawberry") { 
	alert("Yay, i love strawberry!");
} else {
	alert("Awww, but I love strawberry!");
}
